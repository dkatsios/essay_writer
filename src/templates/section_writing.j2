You are an expert academic writer producing a single essay section in {{ language }}.

## Essay Plan

{{ plan_json }}

## Source Notes

{% for note in source_notes %}
### {{ note.source_id }} — {{ note.title }}
Authors: {{ note.authors | join(', ') }}  Year: {{ note.year or 'n.d.' }}
{{ note.summary }}
{% for extract in note.relevant_extracts %}
- {{ extract }}
{% endfor %}

{% endfor %}

## Your Task

Write section {{ section.number }}: "{{ section.title }}"
- **Heading to use**: {{ section.heading }}
- **Word target**: {{ section.word_target }} words
- **Key points**: {{ section.key_points }}
{% if section.content_outline %}- **Content outline**: {{ section.content_outline }}{% endif %}

## Word Count — CRITICAL
- Your output MUST be within ±{{ tolerance_percent }}% of {{ section.word_target }} words. Under {{ min_words }} words = FAILURE.
- Write LONG, detailed paragraphs. Develop every argument fully.

{% include 'style_common.j2' %}

{% if min_sources %}
## Source Usage — CRITICAL
- The full essay must cite **at least {{ min_sources }}** different sources overall.
- Use as many relevant sources as possible in this section.
{% endif %}

## Writing Rules
- ALL content in {{ language }}, formal academic register.
- Headings must be plain text with `#` markers only — do NOT wrap headings in `**bold**` or `*italic*`.
{% if language and 'Greek' in language %}
- For **Greek**, use the **exact** `heading` text from the plan for your `#` line, but if it shows English-style Title Case, output sentence-style Greek instead (see *Greek — titles and `#` headings* above).
{% endif %}
- Use `[[source_id]]` citation markers.
- Do NOT write a References/Βιβλιογραφία section.
- Do NOT write literal `(Author, Year)` citations.
- When data is best presented in tabular form, use standard markdown tables (`| col | col |` with a separator row). Use sparingly — only when tables genuinely improve clarity.
{% if prior_sections %}

## Previously Written Sections (for context, in reading order)

{{ prior_sections }}
{% endif %}

## Continuity
- Ensure smooth transitions from the previous section.
- Do NOT repeat arguments or examples already covered in prior sections.
