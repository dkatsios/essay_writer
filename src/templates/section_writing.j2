You are an expert academic writer producing a single essay section in {{ language }}.

## Essay Plan

{{ plan_json }}

## Source notes (full summaries — section-relevant subset)

The list below is **ranked for this section’s topic**; you still have the **complete catalog** after it.

{% for note in source_notes %}
### {{ note.source_id }} — {{ note.title }}
Authors: {{ note.authors | join(', ') }}  Year: {{ note.year or 'n.d.' }}
{{ note.summary }}
{% for extract in note.relevant_extracts %}
- {{ extract }}
{% endfor %}

{% endfor %}

## All selected sources (compact catalog)

Every id below is in scope for this essay. Cite any of them with `[[source_id]]` where relevant. If a source appears only here (not in the detailed block above), use title and authors as your guide.

{{ source_catalog }}

**Total selected:** {{ total_selected_sources }} sources.

## Source coverage
- Over the **full** essay, cite **every** `source_id` from the catalog at least once where it plausibly supports a claim (skip only if clearly off-topic).
- In **this** section, prefer sources not yet used heavily in prior sections (see context below), and cite **multiple distinct** ids.
{% if assigned_source_ids %}

## Assigned sources for this section — CRITICAL
The following sources were specifically identified as relevant to this section's content. You **MUST** cite each of them at least once in this section:
{% for sid in assigned_source_ids %}
- `{{ sid }}`
{% endfor %}
You may also cite other sources from the catalog where relevant.
{% endif %}

## Your Task

Write section {{ section.number }}: "{{ section.title }}"
- **Heading to use**: {{ section.heading }}
- **Word target**: {{ section.word_target }} words
- **Key points**: {{ section.key_points }}
{% if section.content_outline %}- **Content outline**: {{ section.content_outline }}{% endif %}

## Structure Fidelity — CRITICAL
- This section is part of the approved plan structure. Preserve its role, scope, and placement in that structure.
- Do NOT turn this section into a generic introduction/body/conclusion substitute if the heading or outline is more specific.
- Do NOT absorb content that belongs to other planned sections.

## Word Count — CRITICAL
- Your output MUST be within ±{{ tolerance_percent }}% of {{ section.word_target }} words. Under {{ min_words }} words = FAILURE.
- Write LONG, detailed paragraphs. Develop every argument fully.

{% include 'style_common.j2' %}

## Minimum distinct sources — CRITICAL
- The full essay must cite **at least {{ min_sources }}** different sources overall (matches the pipeline’s selected-source target). Contribute in this section: add markers for ids not yet used upstream when the content fits.
- Use as many relevant sources as possible in this section.

## Writing Rules
- ALL content in {{ language }}, formal academic register.
- Headings must be plain text with `#` markers only — do NOT wrap headings in `**bold**` or `*italic*`.
- Use the planned section heading and keep the section aligned with the plan's custom structure if one exists.
{% if language and 'Greek' in language %}
- For **Greek**, use the **exact** `heading` text from the plan for your `#` line, but if it shows English-style Title Case, output sentence-style Greek instead (see *Greek — titles and `#` headings* above).
{% endif %}
- Use `[[source_id]]` citation markers.
- Do NOT write a References/Βιβλιογραφία section.
- Do NOT write literal `(Author, Year)` citations.
- When data is best presented in tabular form, use standard markdown tables (`| col | col |` with a separator row). Use sparingly — only when tables genuinely improve clarity.
{% if prior_sections %}

## Previously Written Sections (for context, in reading order)

{{ prior_sections }}
{% endif %}

## Continuity
- Ensure smooth transitions from the previous section.
- Do NOT repeat arguments or examples already covered in prior sections.
