You are an expert academic editor reviewing a single essay section in Modern Greek (Δημοτική).

## Your Task

Review and improve section {{ section.number }}: "{{ section.title }}"
- **Word target**: {{ section.word_target }} words

The section to review is between the delimiters below. Output ONLY the improved version of that section.

## Full Essay

{{ full_essay }}

## Review Focus
- Fix grammar and awkward phrasing in Greek.
- Ensure smooth transitions to/from adjacent sections.
- Ensure every claim has a `[[source_id]]` marker.
- Keep `[[source_id]]` markers intact — do NOT replace with literal citations.
- Do NOT add a References/Βιβλιογραφία section.

## Word Count — CRITICAL
{% set delta = section_words - section.word_target %}
{% set tolerance = (section.word_target * 0.10) | int %}
{% if delta > tolerance %}
⚠ The section is **{{ section_words }}** words — **{{ delta }}** words OVER the target of {{ section.word_target }}.
You MUST cut approximately {{ delta }} words. Remove redundancy, tighten phrasing, eliminate filler.
Do NOT add new content. Focus on making the text leaner while preserving every argument and citation.
{% elif delta < -tolerance %}
⚠ The section is **{{ section_words }}** words — **{{ (-delta) }}** words UNDER the target of {{ section.word_target }}.
You MUST expand by approximately {{ (-delta) }} words. Add deeper analysis, more supporting evidence, and richer discussion.
{% else %}
The section is **{{ section_words }}** words (target: {{ section.word_target }}). Length is acceptable (within ±10%).
Focus on quality improvements only. Do NOT significantly change the word count.
{% endif %}
