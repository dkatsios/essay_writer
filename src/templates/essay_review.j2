You are an expert academic editor reviewing and polishing an essay in {{ language }}.

## Assignment Brief

{{ brief_json }}

## Essay Plan

{{ plan_json }}

## Draft Essay

{{ draft_text }}

{% if source_catalog %}
## All selected sources (compact catalog)

{{ source_catalog }}

**Total selected:** {{ total_selected_sources }} sources.
{% endif %}

{% if uncited_ids %}
## Citation Gap — CRITICAL
The draft cites only **{{ total_selected_sources - uncited_ids|length }}** of **{{ total_selected_sources }}** selected sources. The following source ids are **not cited** in the draft:
{% for sid in uncited_ids %}
- `{{ sid }}`
{% endfor %}

You **MUST** weave in `[[source_id]]` citations for as many of these uncited sources as possible — at least enough to reach **{{ min_sources }}** distinct sources total. Find claims, arguments, or data points in the essay where an uncited source naturally supports the text, and add a `[[source_id]]` marker there. Do NOT fabricate content — attach citations to existing arguments where the source is relevant based on its title and authors in the catalog above.
{% endif %}

## Task

Review and improve the draft following the criteria below. Output the complete polished essay.

{% include 'style_common.j2' %}

{% include 'style_review.j2' %}

## Review Focus
- If the draft opens with submission-form or title-page metadata (student name line, course code, course title, assignment title as an administrative block), remove it entirely; keep only the essay title and scholarly body.
- Fix grammar, spelling, and awkward phrasing.
- Tighten prose: eliminate filler, redundancy, and unnecessary qualifiers.
- Headings must be plain text with `#` markers only — remove any `**` or `*` wrapping from headings.
- Preserve **every** existing `[[source_id]]` marker — do NOT remove, drop, or merge them away when tightening prose; do NOT replace them with literal `(Author, Year)` text.
- Do **not** reduce how many **distinct** sources are cited unless you are fixing a clear factual or formatting error.
- Do NOT add a References/Βιβλιογραφία section.
- Preserve the argument structure, tone, and register — improve expression, do not dramatize or intensify.
- Do NOT add vivid, dramatic, or rhetorical flourishes not present in the draft.

## Word Count — CRITICAL
{% set delta = draft_words - target_words %}
{% set tolerance = (target_words * tolerance_ratio) | int %}
{% if delta > tolerance %}
⚠ The draft is **{{ draft_words }}** words — **{{ delta }}** words OVER the target of {{ target_words }}.
You MUST cut approximately {{ delta }} words. Remove redundancy, tighten phrasing, eliminate filler.
Do NOT add new content. Focus on making the text leaner while preserving every argument and citation.
{% elif delta < -tolerance %}
⚠ The draft is **{{ draft_words }}** words — **{{ (-delta) }}** words UNDER the target of {{ target_words }}.
You MUST expand by approximately {{ (-delta) }} words. Elaborate on existing points with more detail or nuance — do NOT introduce new arguments or dramatic language.
{% else %}
The draft is **{{ draft_words }}** words (target: {{ target_words }}). Length is acceptable (within ±{{ tolerance_percent }}%).
Focus on quality improvements only. Do NOT significantly change the total word count.
{% endif %}
